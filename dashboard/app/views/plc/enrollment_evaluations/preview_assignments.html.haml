- course_unit = @enrollment_unit_assignment.plc_course_unit

You will be assigned these modules
- @enrolled_modules.each do |enrolled_module|
  %li
    = enrolled_module.name

%br
You can change your course enrollment here

- options = course_unit.plc_learning_modules.map {|learning_module| [learning_module.name (' - Required' if learning_module.required).to_s, learning_module.id]}
= select_tag 'learning_module_ids', options_for_select(options, selected: @enrolled_modules.pluck(:id), disabled: course_unit.plc_learning_modules.where(required: true).pluck(:id)), {multiple: true, size: [options.size, 25].min, style: 'width: 100%'}

= hidden_field_tag('learning_module_list')
= submit_tag('Confirm Module Selections', id: 'submitEvaluation')